"""HAI MR-05 qualified deterministic runtime package.

Qualified deterministic runtime surfaces are implemented through HAI-MR-22B-R2.
Live and production external execution remain unavailable. The explicit offline CLI supports one-shot composition plus a zero-send prepare/resume boundary using stdin JSON and an already-obtained response; all other CLI use remains fail-closed.
"""

__version__ = '0.0.0'
