"""HAI MR-05 qualified deterministic runtime package.

Qualified deterministic runtime surfaces are implemented through HAI-MR-22A-R2.
Live and production external execution remain unavailable. The explicit offline CLI is bounded to stdin JSON plus an already-obtained response; all other CLI use remains fail-closed.
"""

__version__ = '0.0.0'
